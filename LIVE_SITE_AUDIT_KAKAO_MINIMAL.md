# PROMPT — ผลตรวจสอบเว็บจริง + แผนปรับแต่ละฟังก์ชันให้มินิมอลสไตล์ Kakao Webtoon

> **บริบท:** ไฟล์นี้มาจากการดึงหน้าเว็บจริงที่ nexus-webtoon.vercel.app มาตรวจสอบโครงสร้าง (หน้าแรก, /schedule, /coin-shop, /library, /author) แล้ววิเคราะห์ว่าแต่ละฟังก์ชันขาดอะไร และควรปรับให้มินิมอลสไตล์ Kakao Webtoon อย่างไร — ให้ Gemini อ่านไฟล์นี้คู่กับ `AGENTS.md` (สเปกเต็ม), `FIX_FUNCTIONAL_SYSTEMS.md` (Checklist ทำให้ใช้งานได้จริง) และ `REDESIGN_DISCOVERY_GRID.md` (สเปก Card/Grid แบบละเอียด)

---

## ผลตรวจสอบเว็บจริง (Live Site Audit)

| หน้า | สิ่งที่ทำมาดีแล้ว | สิ่งที่ยังขาด |
|---|---|---|
| หน้าแรก (`/`) | Hero Banner, แท็บประเภท (ทั้งหมด/มังงะ/นิยาย), แท็บแนว (แฟนตาซี/โรแมนติก ฯลฯ), หัวข้อ "ผลงานทั้งหมด" | **กริดผลงานว่างเปล่า** ไม่มีการ์ดเรื่องแสดงจริงแม้แต่เรื่องเดียว |
| ตารางรายวัน (`/schedule`) | แท็บวันจันทร์–อาทิตย์ + END ครบ, มีคอนเซปต์ **"รับตั๋วอ่านฟรีประจำวัน"** ตรงกับ Kakao ตัวจริง (기다무) | ใต้แท็บวันไม่มีรายชื่อเรื่องที่อัปเดตวันนั้นแสดงเลย |
| ร้านค้าเหรียญ (`/coin-shop`) | แยกยอด "ซื้อ" กับ "ฟรี" ถูกต้องตามสเปก, Empty State ประวัติธุรกรรมเขียนดี ("ยังไม่มีประวัติการทำรายการ") | **หัวข้อ "แพ็กเกจเหรียญ" ว่างเปล่า** ไม่มีแพ็กเกจให้เลือกซื้อแม้แต่อันเดียว |
| ชั้นหนังสือ (`/library`) | Empty/Unauthenticated State มีข้อความอธิบายชัดเจน | มีแค่ปุ่ม "กลับหน้าหลัก" — **ไม่มีปุ่มเข้าสู่ระบบตรงจุด** ทั้งที่ข้อความบอกให้ล็อกอิน ผู้ใช้ต้องเดินย้อนไปหาเองที่ Navbar |
| สตูดิโอนักเขียน (`/author`) | มี **Auth Guard ทำงานจริง** — ผู้ใช้ที่ไม่ใช่นักเขียนถูก Redirect กลับหน้าแรกพร้อม Parameter `?auth_error=unauthorized_author` | **ไม่มีข้อความแจ้งเตือนใดๆ ปรากฏให้ผู้ใช้เห็น** ว่าทำไมถูกเด้งกลับ — ผู้ใช้จะงงว่าเกิดอะไรขึ้น และไม่มีทางกดสมัครเป็นนักเขียนจากจุดนี้ต่อได้เลย |
| หน้ากฎหมาย | ลิงก์ ข้อตกลงการใช้งาน/นโยบายความเป็นส่วนตัว/ข้อตกลงนักเขียน ครบและเข้าถึงได้จาก Footer | ยังไม่ได้ตรวจเนื้อหาจริงข้างในว่าเป็นเนื้อหาจริงหรือหน้าเปล่า |

**สรุปภาพรวม:** โครงหน้า (Skeleton/Routing/Auth Guard) ถูกวางไว้ดีและตรงสเปกที่วางแผนไว้แล้ว ปัญหาหลักตอนนี้**ไม่ใช่เรื่องดีไซน์** แต่เป็น **(1) ไม่มีข้อมูลจริงมาเติมกริด/แพ็กเกจ** และ **(2) มี Logic การเช็คสิทธิ์อยู่แล้วแต่ขาด UI แจ้งผลลัพธ์ให้ผู้ใช้เห็น** — สองจุดนี้ควรแก้ก่อนเริ่มปรับดีไซน์ให้สวยขึ้น เพราะต่อให้ดีไซน์สวยแค่ไหน หน้าที่ว่างเปล่าก็ยังใช้งานไม่ได้อยู่ดี

---

## แผนปรับแต่ละฟังก์ชันให้มินิมอลสไตล์ Kakao (เรียงตามลำดับที่ควรทำ)

### ฟังก์ชัน 1: กริดผลงานหน้าแรก
- **แก้ก่อน**: เชื่อมกริดกับข้อมูลจริงจากฐานข้อมูล (เรื่องที่สถานะเผยแพร่แล้วเท่านั้น)
- **ดีไซน์**: ใช้สเปก Card/Grid ตาม `REDESIGN_DISCOVERY_GRID.md` ทั้งหมด (ปกอัตราส่วน 3:4, Badge สูงสุด 1 อันต่อการ์ด, กริด 5–6 คอลัมน์ Desktop)

### ฟังก์ชัน 2: ตารางรายวัน (Schedule)
- **แก้ก่อน**: ใต้แท็บวันแต่ละวันต้องดึงรายชื่อเรื่องที่ตั้งค่าให้อัปเดตวันนั้นจริง (ต้องมี field `release_day` ในตาราง `stories` หรือ `chapters`)
- **ดีไซน์แบบมินิมอล**: การ์ด "ตั๋วอ่านฟรีประจำวัน" ให้เป็นกล่องเรียบๆ ใบเดียวด้านบนสุด มีไอคอนตั๋ว + ปุ่ม "รับตั๋ว" + ข้อความบอกจำนวนที่มี — **ไม่ต้องมี Animation เปิดกล่องของขวัญแบบเกม** (Kakao ของจริงมีภาพเคลื่อนไหวหวือหวา ตัดออกให้เหลือแค่ปุ่มกด + Toast แจ้งผลสั้นๆ พอ)
- ใต้การ์ดตั๋ว ใช้กริดเดียวกับฟังก์ชัน 1 กรองตามวันที่เลือก

### ฟังก์ชัน 3: ร้านค้าเหรียญ (Coin Shop)
- **แก้ก่อน**: เพิ่มข้อมูลแพ็กเกจเหรียญจริง (ตาราง `coin_packages` ต้องมีข้อมูล seed อย่างน้อย 4–5 แพ็กเกจ) และ Query มาแสดงในหน้านี้
- **ดีไซน์แบบมินิมอล**: การ์ดแพ็กเกจเรียบๆ เรียงกริด 4–5 ใบ ต่อแถว แต่ละใบมีแค่ **ไอคอนเหรียญ + จำนวนเหรียญตัวเลขใหญ่ + ราคา** ไม่ต้องมีภาพกองเหรียญ/ป้ายลดราคาสีฉูดฉาดแบบแอปเกมทั่วไป ถ้าจะมีแพ็กเกจ "คุ้มที่สุด" ให้ใช้แค่กรอบ Accent Color บางๆ ล้อมการ์ดใบนั้นใบเดียวพอ
- ประวัติธุรกรรม: List เรียบๆ ต่อยอดจาก Empty State ที่มีอยู่แล้ว แสดง วันที่ / รายการ / จำนวนเหรียญ +/- ในแถวเดียว

### ฟังก์ชัน 4: ชั้นหนังสือ (Library)
- **แก้ก่อน**: เพิ่มปุ่ม "เข้าสู่ระบบ" ในหน้า Empty State ตรงนี้เลย (ไม่ต้องให้ผู้ใช้กดกลับหน้าหลักไปหาปุ่ม Login เอง — ลดขั้นตอนจาก 2 คลิกเหลือ 1 คลิก)
- **ดีไซน์**: เมื่อ Login แล้ว ใช้กริดสไตล์เดียวกับฟังก์ชัน 1 แต่มีแท็บย่อยมินิมอล 3 แท็บ: กำลังอ่าน / บุ๊คมาร์ค / ซื้อแล้ว

### ฟังก์ชัน 5: สตูดิโอนักเขียน (Author Studio)
- **แก้ก่อน (สำคัญที่สุดในหมวดนี้)**: ตอนนี้ Auth Guard ทำงานถูกต้องแล้ว แต่ต้องเพิ่ม Logic รับค่า `auth_error=unauthorized_author` จาก URL แล้วแสดงผลจริง:
  - ถ้า**ยังไม่ได้ล็อกอินเลย** → Redirect ไปหน้า Login
  - ถ้า**ล็อกอินแล้วแต่ยังไม่ใช่ Author** → แสดงหน้า "สมัครเป็นนักเขียน" ทันทีที่ `/author` เอง (ใช้ Flow ตามหมวด E ใน `FIX_FUNCTIONAL_SYSTEMS.md`) แทนการเด้งกลับหน้าแรกเฉยๆ แบบตอนนี้
- **ดีไซน์**: หน้าสมัครเป็นนักเขียนที่ปรากฏตรงนี้ให้เป็นฟอร์มเรียบๆ ทีละขั้น (Step 1 ข้อมูลนามปากกา → Step 2 ข้อมูลรับเงิน → Step 3 ยืนยัน) ไม่ต้องยัดทุกช่องกรอกในหน้าเดียว

### ฟังก์ชัน 6: ค้นหา (Search)
- ยังไม่ได้ตรวจสอบเนื้อหาในไฟล์นี้ — แนะนำให้ Gemini ตรวจ `/search` ต่อว่าผลลัพธ์การค้นหาดึงข้อมูลจริงหรือยัง แล้วใช้ Card/Grid สไตล์เดียวกับฟังก์ชัน 1 ในการแสดงผลลัพธ์

---

## หลักมินิมอลที่ใช้ร่วมกันทุกฟังก์ชัน (สรุปสั้น)

1. **Badge/ป้ายสถานะ**: สูงสุด 1 ป้ายต่อรายการเสมอ ไม่ว่าจะเป็นการ์ดเรื่อง, แพ็กเกจเหรียญ, หรือสถานะตอน
2. **สี**: โทนกลาง (ขาว/ดำ/เทา) เป็นหลัก + Accent Color สีเดียวของเว็บ ใช้ซ้ำทุกที่แทนสีสันหลากหลายแบบ Kakao
3. **Animation**: ใช้ Fade/Scale เบาๆ เท่านั้น ตัด Animation หวือหวาแบบเกม (กล่องของขวัญเปิด, เหรียญกระเด็น ฯลฯ) ออกทั้งหมด
4. **ทุก Empty/Error State ต้องมี Action ให้กดต่อได้ทันที** ไม่ใช่แค่ข้อความเฉยๆ (ตามที่เจอปัญหาในหน้า Library และ Author ด้านบน)

---

## ลำดับที่แนะนำให้สั่ง Gemini ทำ (ทีละคำสั่ง ประหยัด Token)

1. "เชื่อมกริดหน้าแรกกับข้อมูลจริงจากฐานข้อมูล ตามฟังก์ชัน 1 ในไฟล์นี้"
2. "เพิ่ม Seed ข้อมูลแพ็กเกจเหรียญ 5 แพ็กเกจ และแสดงในหน้า coin-shop ตามฟังก์ชัน 3"
3. "แก้หน้า /author ให้แสดงฟอร์มสมัครนักเขียนเมื่อเจอ auth_error=unauthorized_author แทนการเด้งกลับเฉยๆ ตามฟังก์ชัน 5"
4. "เพิ่มปุ่มเข้าสู่ระบบในหน้า library ตามฟังก์ชัน 4"
5. "ปรับดีไซน์กริดผลงานทั้งหมดให้เป็นสไตล์มินิมอลตามหลักในหัวข้อสุดท้ายของไฟล์นี้"
