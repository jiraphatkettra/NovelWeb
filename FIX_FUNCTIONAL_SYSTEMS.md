# PROMPT — เติมระบบให้ใช้งานได้จริง (Functional Implementation Gaps)

> **บริบทสำหรับ Gemini:** หน้าเว็บ (UI/Layout) ของระบบถูกสร้างไว้แล้ว แต่ฟังก์ชันจริงเบื้องหลังของแต่ละบทบาท (นักเขียน/ผู้อ่าน/แอดมิน) ยังใช้งานไม่ได้จริง — ปุ่มกดไม่มีผล ไม่มีการบันทึกข้อมูลจริง ไม่มีเครื่องมือจัดการเนื้อหาที่ใช้งานได้ นี่คือไฟล์เฉพาะสำหรับ**อุดช่องว่างให้ระบบทำงานได้จริงแบบ End-to-End** ไม่ใช่การออกแบบสถาปัตยกรรมใหม่ตั้งแต่ต้น
>
> **กติกาการทำงาน:**
> 1. ตรวจสอบโค้ด/หน้าเว็บที่มีอยู่ก่อนเสมอ — ถ้าหน้า UI มีอยู่แล้วให้ต่อ Logic เข้ากับของเดิม อย่าสร้างหน้าใหม่ซ้ำ
> 2. ทำทีละหมวดตามลำดับ A → B → C ด้านล่าง อย่าพยายามทำทุกอย่างพร้อมกัน
> 3. แต่ละข้อ Checklist คือ "งานที่ทำเสร็จแล้วต้องกดใช้งานได้จริงจากหน้าเว็บ" ไม่ใช่แค่มี Component โชว์เฉยๆ
> 4. หากต้องอ้างอิงรายละเอียด Schema/API/สถาปัตยกรรมเต็มรูปแบบ ให้ดูไฟล์ `AGENTS.md` ที่ให้ไว้ก่อนหน้า — ไฟล์นี้คือสิ่งที่ต้อง "ทำให้เสร็จก่อน" จากสิ่งที่ระบุไว้ในนั้น

---

## ลำดับความสำคัญ (ทำตามนี้ก่อน)

1. **ระบบเขียน/จัดการผลงานของนักเขียน** — สำคัญที่สุด เพราะไม่มีเนื้อหา = เว็บใช้งานไม่ได้เลย
2. **ระบบปลดล็อกเนื้อหา + กระเป๋าเหรียญของผู้อ่าน** — ต้องเชื่อมกับเนื้อหาจริงจากข้อ 1
3. **ระบบจัดการของแอดมิน** — ต้องมีเนื้อหาจริงให้ตรวจสอบก่อนถึงจะทดสอบได้
4. **ระบบมีส่วนร่วม** (คอมเมนต์/บุ๊คมาร์ค/ติดตาม) — เสริมหลังแกนหลักทำงานได้แล้ว

---

## A. ระบบนักเขียน (Author — Writing & Content Management)

นี่คือเครื่องมือที่นักเขียนใช้งานจริงทุกวัน ต้องออกแบบให้ใช้ง่ายเหมือนเขียนบล็อกหรือเอกสาร ไม่ใช่แค่ฟอร์มกรอกข้อมูล

### A.1 แดชบอร์ดผลงานของฉัน (My Works Dashboard)
- [ ] หน้ารวมผลงานทั้งหมดของนักเขียนคนนั้น แสดงเป็นการ์ด: ปกเรื่อง, ชื่อเรื่อง, สถานะ (ฉบับร่าง/กำลังลง/จบแล้ว/พักเรื่อง/รอตรวจสอบ), จำนวนตอนที่ลงแล้ว, ยอดวิวรวม
- [ ] ปุ่ม "สร้างเรื่องใหม่" ที่กดแล้วพาไปหน้าสร้างเรื่องจริง (ไม่ใช่ Placeholder)
- [ ] แต่ละการ์ดกดแล้วเข้าไปจัดการเรื่องนั้นได้ (แก้ข้อมูลเรื่อง/จัดการตอน/ดูสถิติ)
- [ ] ปุ่มลบ/เก็บเข้าคลัง (Archive) เรื่องที่ไม่ต้องการแสดงแล้ว ทำงานจริงและลบ/ซ่อนจากหน้าเว็บสาธารณะทันที

### A.2 สร้างเรื่องใหม่ (Create Story Flow)
- [ ] ฟอร์มสร้างเรื่อง: ชื่อเรื่อง, อัปโหลดปก (พร้อม Preview ก่อนบันทึกจริง), เรื่องย่อ, เลือกประเภท (นิยาย/มังงะ) — **เลือกได้ครั้งเดียวตอนสร้าง เพราะกำหนดว่าตอนถัดไปจะเป็น Text Editor หรือ Image Uploader**
- [ ] เลือกแนว/แท็ก (Multi-select จากรายการที่มีอยู่จริงในระบบ ไม่ใช่ Text ฟรี)
- [ ] กำหนดเรตอายุเนื้อหา (All Ages/13+/18+)
- [ ] กดบันทึกแล้วต้องสร้างเรื่องจริงในฐานข้อมูล และพาไปหน้าจัดการตอนของเรื่องนั้นทันที (ยังไม่ต้องเผยแพร่)

### A.3 ระบบจัดการตอน (Chapter Manager) — ต่อเรื่อง
- [ ] รายการตอนทั้งหมดของเรื่องนั้น แบบตาราง/ลิสต์ แสดง: ลำดับตอน, ชื่อตอน, จำนวนคำ/หน้า, ราคา (ฟรี/กี่เหรียญ), สถานะ (ร่าง/ตั้งเวลา/เผยแพร่แล้ว), วันที่เผยแพร่
- [ ] **ลากเพื่อสลับลำดับตอน (Drag-to-Reorder)** และบันทึกลำดับใหม่ทันทีที่ปล่อยเมาส์
- [ ] ปุ่ม "เพิ่มตอนใหม่" ที่พาไปหน้า Editor ที่ตรงกับประเภทเรื่อง (นิยาย→Text Editor / มังงะ→Image Uploader)
- [ ] แต่ละตอนกดแก้ไข/ดูตัวอย่าง (Preview เหมือนที่ผู้อ่านจะเห็น)/ลบ/ทำสำเนา (Duplicate) ได้จริง

### A.4 Editor เขียนนิยาย (Text Chapter Editor) — สำหรับเรื่องประเภทนิยาย
- [ ] ช่องชื่อตอน + กล่องเขียนเนื้อหาแบบ Rich Text จริง (ตัวหนา/ตัวเอียง/หัวข้อ/ย่อหน้า/เส้นคั่น) ไม่ใช่ Textarea เปล่า
- [ ] **Autosave**: บันทึกร่างอัตโนมัติทุก 15–30 วินาที หรือเมื่อหยุดพิมพ์ พร้อมข้อความแจ้ง "บันทึกแล้ว" ให้เห็นชัดเจน
- [ ] กู้คืนร่างอัตโนมัติหากปิดเบราว์เซอร์/เน็ตหลุดกลางคัน (สำรองไว้ใน Local Storage คู่กับ Server เผื่อกรณีฉุกเฉิน)
- [ ] แสดงจำนวนคำและเวลาอ่านโดยประมาณแบบ Real-time ขณะพิมพ์
- [ ] ปุ่ม "ดูตัวอย่าง" เปิดหน้าอ่านจริงในแท็บ/โมดัลใหม่ก่อนเผยแพร่จริง

### A.5 ตัวอัปโหลดมังงะ (Manga Page Uploader) — สำหรับเรื่องประเภทมังงะ
- [ ] โซนลากไฟล์ภาพหลายไฟล์พร้อมกัน (Drag & Drop Multi-upload) รองรับ JPG/PNG/WebP
- [ ] แสดง Progress Bar ระหว่างอัปโหลดแต่ละไฟล์ และ Thumbnail Grid ของหน้าที่อัปโหลดแล้ว
- [ ] **ลากสลับลำดับหน้า (Reorder Pages)** ก่อนเผยแพร่ — สำคัญมาก เพราะลำดับหน้าผิดพลาดง่ายจากการอัปโหลดพร้อมกันหลายไฟล์
- [ ] กดลบ/แทนที่ภาพเฉพาะหน้าได้โดยไม่ต้องอัปโหลดใหม่ทั้งหมด
- [ ] ระบบบีบอัด/ปรับขนาดภาพอัตโนมัติหลังอัปโหลด (ลดขนาดไฟล์โดยไม่เสียคุณภาพเห็นชัด)
- [ ] ปุ่ม "ดูตัวอย่าง" แสดงผลแบบเลื่อนอ่านต่อเนื่องเหมือนหน้าอ่านจริงของผู้ใช้

### A.6 เผยแพร่และตั้งราคา (Publish & Pricing) — ใช้ร่วมกันทั้งนิยาย/มังงะ
- [ ] ตั้งราคาตอน: ฟรี หรือ ระบุจำนวนเหรียญ (Input ตัวเลขจริง ไม่ใช่ Dropdown ตายตัว)
- [ ] ตัวเลือกเผยแพร่: **เผยแพร่ทันที** / **ตั้งเวลาเผยแพร่ล่วงหน้า** (Date-Time Picker จริง ระบบต้องมี Job ที่เผยแพร่อัตโนมัติเมื่อถึงเวลา) / **บันทึกเป็นร่าง**
- [ ] เมื่อกดเผยแพร่จริง ต้องอัปเดตสถานะในฐานข้อมูลและ**ปรากฏบนหน้าเว็บสาธารณะทันที** ให้ผู้อ่านเห็นและปลดล็อกได้จริง

### A.7 รายได้และถอนเงิน (Earnings & Payout) — ต้องคำนวณจากข้อมูลจริง ไม่ใช่ Mock
- [ ] แดชบอร์ดรายได้ดึงตัวเลขจริงจากประวัติการปลดล็อกตอน (ไม่ใช่ตัวเลขคงที่ที่ Hardcode ไว้)
- [ ] กราฟยอดขายต้องอัปเดตตามธุรกรรมจริงที่เกิดขึ้น
- [ ] ฟอร์มขอถอนเงิน: กรอกจำนวน (ไม่เกินยอดคงเหลือจริง) ผูกกับข้อมูลบัญชีธนาคารที่บันทึกไว้ในโปรไฟล์ กดส่งคำขอแล้วต้องไปปรากฏในคิวอนุมัติของแอดมินจริง

---

## B. ระบบผู้อ่าน (Reader — ทำให้ใช้งานได้จริง)

### B.1 กระเป๋าเหรียญและการซื้อเหรียญ
- [ ] หน้ากระเป๋าเงินแสดงยอดเหรียญจริงจากฐานข้อมูล ไม่ใช่ตัวเลขคงที่
- [ ] เลือกแพ็กเกจ → เปิดหน้า/โมดัลชำระเงิน (ใช้ Sandbox ของ Payment Gateway ได้ในระยะนี้) → เมื่อจ่ายสำเร็จ ยอดเหรียญต้องเพิ่มขึ้นจริงและอัปเดตหน้าจอทันทีโดยไม่ต้อง Refresh เอง

### B.2 ปลดล็อกตอน (Chapter Unlock)
- [ ] กดปุ่ม "ปลดล็อกด้วย X เหรียญ" → ระบบเช็คยอดเหรียญคงเหลือจริง → หากพอ หักเหรียญจริงในฐานข้อมูล → บันทึกว่าผู้ใช้คนนี้ปลดล็อกตอนนี้แล้ว → แสดงเนื้อหาเต็มทันทีโดยไม่ต้องโหลดหน้าใหม่
- [ ] หากเหรียญไม่พอ ต้องแจ้งเตือนและมีปุ่มลิงก์ไปหน้าเติมเหรียญทันที
- [ ] ตอนที่ปลดล็อกแล้วต้องจำสถานะไว้ถาวร (เข้ามาอ่านซ้ำได้โดยไม่ต้องจ่ายเหรียญอีก)

### B.3 ห้องสมุดส่วนตัว / บุ๊คมาร์ค / ประวัติการอ่าน
- [ ] กดบุ๊คมาร์คเรื่องแล้วต้องไปปรากฏในหน้า "ห้องสมุดของฉัน" จริง (บันทึกลง DB ผูกกับ User ไม่ใช่แค่ State ชั่วคราวในเบราว์เซอร์)
- [ ] บันทึกตำแหน่งตอนล่าสุดที่อ่านของแต่ละเรื่องอัตโนมัติ และมีปุ่ม "อ่านต่อ" พาไปยังจุดที่ค้างไว้พอดี

### B.4 คอมเมนต์ / เรตติ้ง / ติดตามนักเขียน
- [ ] โพสต์คอมเมนต์แล้วบันทึกจริงและแสดงผลทันที (ไม่ใช่ตัวอย่างคอมเมนต์ที่ Hardcode ไว้ในหน้า)
- [ ] ให้คะแนนดาวแล้วบันทึกจริงและคำนวณค่าเฉลี่ยใหม่ให้ถูกต้อง
- [ ] ปุ่มติดตามนักเขียนต้องสลับสถานะ Follow/Unfollow จริง และมีผลต่อ Feed แจ้งเตือนตอนใหม่จริง

---

## C. ระบบแอดมิน (Admin — ทำให้ใช้งานได้จริง)

### C.1 จัดการผู้ใช้
- [ ] ค้นหา/กรองผู้ใช้จริงจากฐานข้อมูล
- [ ] กดระงับบัญชีแล้วผู้ใช้คนนั้นต้อง Login ไม่ได้จริงในทันที

### C.2 ตรวจสอบและอนุมัติเนื้อหา
- [ ] คิวเรื่อง/ตอนที่รอตรวจสอบต้องดึงจากเรื่องที่นักเขียนกดเผยแพร่จริงในข้อ A.6 (ถ้าเปิดใช้ระบบตรวจก่อนเผยแพร่)
- [ ] กดอนุมัติแล้วเรื่องต้องไปปรากฏบนหน้าเว็บสาธารณะทันที, กดปฏิเสธแล้วต้องแจ้งเหตุผลกลับไปหานักเขียนจริง

### C.3 จัดการเหรียญและการเงิน
- [ ] แก้ไข/เพิ่มแพ็กเกจเหรียญแล้วต้องไปเปลี่ยนตัวเลือกที่ผู้อ่านเห็นในหน้าซื้อเหรียญจริง (ข้อ B.1) ทันที
- [ ] อนุมัติคำขอถอนเงินของนักเขียน (จากข้อ A.7) แล้วต้องอัปเดตสถานะและยอดคงเหลือของนักเขียนจริง

---

## D. Definition of Done — ทดสอบว่า "ใช้งานได้จริง" แล้วหรือยัง

ให้ทดสอบ 3 สถานการณ์นี้แบบ End-to-End ก่อนถือว่างานเสร็จ:

1. **นักเขียน**: สมัคร → สร้างเรื่องใหม่ → เขียนตอนที่ 1 จริงในกล่องข้อความ (หรืออัปโหลดภาพมังงะจริง) → ตั้งราคา 10 เหรียญ → กดเผยแพร่ → ตอนต้องไปปรากฏบนหน้าเว็บให้คนอื่นเห็นได้จริง
2. **ผู้อ่าน**: เห็นตอนใหม่จากข้อ 1 → เติมเหรียญ (Sandbox) → กดปลดล็อกตอนนั้นด้วยเหรียญที่มี → เหรียญถูกหักจริง → อ่านเนื้อหาเต็มได้ทันที → บุ๊คมาร์คเรื่องไว้ → คอมเมนต์ได้จริง
3. **แอดมิน**: เห็นเรื่องใหม่จากข้อ 1 ในคิวตรวจสอบ (ถ้าเปิดระบบนี้) → กดอนุมัติ → เห็นธุรกรรมเหรียญจากข้อ 2 ในหน้าธุรกรรม → เมื่อนักเขียนขอถอนเงิน กดอนุมัติได้จริง

หากทำ 3 Flow นี้ได้ครบแบบไม่มีจุดไหน "กดแล้วไม่มีอะไรเกิดขึ้น" ถือว่าระบบแกนหลักใช้งานได้จริงแล้ว จากนั้นค่อยขยายไปทำฟีเจอร์เสริมอื่นๆ ตาม `AGENTS.md`
